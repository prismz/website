# make sure dirs are in path
PATH=$PATH:/$HOME/.local/bin
PATH=$PATH:/sbin/

# shell prompt
PS1="[\W] λ "

# ls aliases
alias ll='ls -l'
alias la='ls -a'
alias lla='ls -la'
alias l1='ls -1'
alias sl='ls' # for typos

# apt aliases
alias p='sudo apt install'
alias ps='sudo apt search'
alias pr='sudo apt remove'
alias update='sudo apt-get update && sudo apt-get upgrade'
alias depclean='sudo apt autoremove'

# aurinstall aliases (enable on arch)
# alias y='aurinstall install'
# alias ys='aurinstall search'
# alias update='aurinstall update'

# config file aliases
alias bashrc='nvim ~/.bashrc && source ~/.bashrc'
alias vimrc='nvim ~/.vimrc'

# command replacements
alias cls='clear'
alias cp='cp -r'
alias c='cat'
alias nv='nvim'
alias n='nvim'
alias ht='htop'
alias cm='cmus'
alias sx='cd && startx'
alias rb_='sudo reboot'
alias murder='killall -9'
alias obliterate='srm -rvz'
alias quick-obliterate='srm -rlvz'
alias ssh='TERM="xterm" ssh'
alias addkey='gpg --recv-keys'

# youtube-dl aliases
alias ytdl='youtube-dl'
alias yt='youtube-dl'
alias ytda='youtube-dl -x --audio-format mp3'

# directory aliases
alias music='cd ~/hdd1/Music/'
alias devel='cd ~/development'
alias dl='cd ~/downloads'
alias docs='cd ~/docs'
alias soft='cd ~/software'

# set neovim as editor
EDITOR=nvim
export EDITOR=nvim

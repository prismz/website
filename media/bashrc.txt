# make sure dirs are in path
PATH=$PATH:/$HOME/.local/bin
PATH=$PATH:/sbin/
PATH=$PATH:/$HOME/.cargo/bin

# shell prompt
PS1="[\W] λ "

# ls aliases
alias ll='ls -lh'
alias la='ls -a'
alias lla='ls -lah'
alias l1='ls -1'
alias sl='ls' # for typos

# apt aliases
alias p='sudo pacman -S'
alias ps='sudo pacman -Ss'
alias pr='sudo pacman -R'
alias pq='pacman -Q'
alias update='sudo pacman -Syu'
alias depclean='sudo pacman -Rsn $(sudo pacman -Qqtd)'

# aurinstall aliases (enable on arch)
alias y='aurinstall install'
alias ys='aurinstall search'
alias update='sudo pacman -Syu && aurinstall update'

# config file aliases
alias bashrc='nvim ~/.bashrc && source ~/.bashrc'
alias vimrc='nvim ~/.vimrc'

# command replacements
alias cls='clear'
alias cp='cp -rv'
alias c='cat'
alias nv='nvim'
alias n='nvim'
alias ht='htop'
alias cm='cmus'
alias sx='cd && startx'
alias rb_='sudo reboot'
alias murder='killall -9'
alias obliterate='srm -rvz'
alias quick-obliterate='srm -rlvz'
alias ssh='TERM="xterm" ssh'
alias addkey='gpg --recv-keys'
alias python='python3'
alias sl='slock'
alias up='uptime'
alias rm='rm -rfv'
alias neo='neofetch'
alias hackerman='clear; neofetch'
alias server='TERM="xterm" ssh root@hasanzahra.xyz'

# youtube-dl aliases
alias ytdl='youtube-dl'
alias yt='youtube-dl'
alias ytda='youtube-dl -x --audio-format mp3'

# directory aliases
alias mus='cd ~/aud/mus/'

# set neovim as editor
EDITOR=nvim
export EDITOR=nvim

if [ $TERM = "st-256color" ]
then
    tput smkx
fi

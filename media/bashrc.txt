# make sure dirs are in path
PATH=$PATH:/$HOME/.local/bin
PATH=$PATH:/sbin/

# shell prompt
PS1="[\W] λ "

# ls aliases
alias ll='ls -lh'
alias la='ls -a'
alias lla='ls -lah'
alias l1='ls -1'
alias sl='ls' # for typos

# portage aliases
alias e='doas emerge --ask'
alias es='doas emerge --search'
alias er='doas emerge --deselect'
alias eq='~/dev/portage-utils/portage-installed'
alias eu='doas emerge-webrsync && doas emerge --ask --update --deep --with-bdeps=y --newuse @world'
alias ed='doas emerge --depclean --ask'
alias fixtty='printf "\e]2;st\a" && tput smkx'  # emerge has a tendency to mess up st's titles.
alias whatscompiling='watch -cn 3 doas genlop -ci'
alias compile_kernel='cd /usr/src/linux \
    && doas make modules_prepare && doas emerge --ask @module-rebuild && doas make -j8 && doas make modules_install && doas make install && doas grub-mkconfig -o /boot/grub/grub.cfg && doas genkernel --install --kernel-config=/usr/src/linux/.config initramfs'

# aurinstall aliases (enable on arch)
# alias y='aurinstall install'
# alias ys='aurinstall search'
# alias update='doas pacman -Syu && aurinstall update'

# gentoo aliases
alias kernel='cd /usr/src/linux'
alias makeconf='doas nvim /etc/portage/make.conf'
alias portage='cd /etc/portage'

# config file aliases
alias bashrc='nvim ~/.bashrc && source ~/.bashrc'
alias vimrc='nvim ~/.vimrc'

# command replacements
alias cls='clear'
alias cp='cp -rv'
alias c='cat'
alias nv='nvim'
alias n='nvim'
alias ht='htop'
alias cm='cmus'
alias sx='cd && startx'
alias rb_='doas reboot'
alias murder='killall -9'
alias obliterate='srm -rvz'
alias quick-obliterate='srm -rlvz'
alias ssh='TERM="xterm" ssh'
alias addkey='gpg --recv-keys'
alias python='python3'
alias up='uptime'
alias rm='rm -rfv'
alias neo='neofetch'
alias hackerman='clear; neofetch'
alias server='TERM="xterm" ssh root@hasanzahra.xyz'
alias mullvad='cd ~/dl/mullvad_config_linux_us_atl/; doas openvpn --config /etc/openvpn/openvpn.conf'
alias amiconnected='curl https://am.i.mullvad.net/connected'

# youtube-dl aliases
alias ytdl='youtube-dl'
alias yt='youtube-dl'
alias ytda='youtube-dl -x --audio-format mp3'

# directory aliases
alias mus='cd ~/aud/mus/'

# set neovim as editor
EDITOR=nvim
export EDITOR=nvim

tput smkx

echo "$(whoami)@$(hostname) $(uname -rm)"
